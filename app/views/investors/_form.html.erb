<%= form_for(@investor) do |f| %>
  <% if @investor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@investor.errors.count, "error") %> prohibited this investor from being saved:</h2>

      <ul>
      <% @investor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :total_cost %><br />
    <%= f.number_field :total_cost %>
  </div>
  <div class="field">
    <%= f.label :total_value %><br />
    <%= f.number_field :total_value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
